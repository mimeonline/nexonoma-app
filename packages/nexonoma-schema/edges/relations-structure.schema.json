{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nexonoma.de/schema/relations.structure.schema.json",
  "title": "Nexonoma Structure Relations",
  "description": "Relationsschema für Strukturelle Zusammenhänge im Nexonoma Wissensgraph. Wird für hierarchische, aggregierende und strukturbildende Beziehungen verwendet.",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://nexonoma.de/schema/relations.base.schema.json"
    },
    {
      "properties": {
        "relation": {
          "type": "string",
          "enum": ["BELONGS_TO", "CONTAINS"],
          "enumDescriptions": [
            "BELONGS_TO: Dieses Element ist Teil eines übergeordneten Blocks (z. B. ein Segment gehört zu einem Cluster).",
            "CONTAINS: Das Element enthält andere Bausteine als Bestandteile."
          ],
          "description": "Strukturelle Relation innerhalb des Wissensmodells."
        }
      }
    }
  ]
}
